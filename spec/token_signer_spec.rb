# frozen_string_literal: true

RSpec.describe TokenSigner do
  it 'has a version number' do
    expect(TokenSigner::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
